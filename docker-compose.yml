services:
  emoji-kitchen-mcp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: emoji-kitchen-mcp
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOST=0.0.0.0
      - TRANSPORT=sse
    restart: unless-stopped
